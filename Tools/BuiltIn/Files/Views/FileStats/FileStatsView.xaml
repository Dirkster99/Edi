<UserControl x:Class="Files.Views.FileStats.FileStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cmdLabel="clr-namespace:Util.Local;assembly=Util"

             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <StackPanel Orientation="Vertical" >
      <TextBlock Text="{Binding FileName}" Margin="3" ToolTip="{x:Static cmdLabel:Strings.STR_FILESTATES_FileName}"/>
      <TextBlock Text="{Binding FilePath}" Margin="3" ToolTip="{x:Static cmdLabel:Strings.STR_FILESTATES_FilePath}"/>

      <TextBlock Text="{Binding FileSize}" Margin="3" ToolTip="{x:Static cmdLabel:Strings.STR_FILESTATES_FileSize}"/>
      <TextBlock Text="{Binding LastModified}" Margin="3" ToolTip="{x:Static cmdLabel:Strings.STR_FILESTATES_LASTMODIFICATION}"/>
    </StackPanel>
  </Grid>
</UserControl>

